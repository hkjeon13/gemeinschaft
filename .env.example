# App
APP_HOST=0.0.0.0
APP_PORT=8000
APP_WORKERS=1

# Auth/JWT
JWT_SECRET_KEY=replace-with-32-plus-char-secret
JWT_ALGORITHM=HS256
JWT_ISSUER=gemeinschaft-api
JWT_AUDIENCE=gemeinschaft-clients
JWT_ACCESS_TOKEN_EXPIRES_MINUTES=60
JWT_REFRESH_TOKEN_EXPIRES_DAYS=14

# Login rate limit
AUTH_LOGIN_MAX_ATTEMPTS=5
AUTH_LOGIN_WINDOW_SECONDS=60
AUTH_LOGIN_BLOCK_SECONDS=300
AUTH_TRUST_PROXY_HEADERS=false

# User credentials (bcrypt hashes recommended)
# Example hash generation:
# python -c "import bcrypt; print(bcrypt.hashpw(b'alice-pass', bcrypt.gensalt()).decode())"
AUTH_USERS_JSON={"alice":{"password_hash":"$2b$12$replace_me","role":"member"}}
AUTH_ALLOW_PLAINTEXT_PASSWORDS=false

# Postgres (preparation phase)
DATABASE_ENABLED=false
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=gemeinschaft
POSTGRES_USER=gemeinschaft
POSTGRES_PASSWORD=change-me
# Optional override:
# DATABASE_URL=postgresql://gemeinschaft:change-me@postgres:5432/gemeinschaft
